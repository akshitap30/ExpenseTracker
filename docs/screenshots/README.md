Place screenshots of the Swagger UI and API responses here.

Suggested screenshots:
- swagger-ui.png     → Swagger UI overview (http://localhost:8080/swagger-ui/index.html)
- auth-endpoints.png → Authentication section expanded in Swagger
- expenses-list.png  → GET /expenses response example
- add-expense.png    → POST /expenses request/response example

To capture:
1. Start the application locally
2. Open http://localhost:8080/swagger-ui/index.html
3. Take screenshots and save them in this directory
